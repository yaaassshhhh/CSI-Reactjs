const conf = {
    SpotifyUrl : String(import.meta.env.VITE_BASE_URL),
    ClientId : String(import.meta.env.VITE_CLIENT_ID),
    SecretId : String(import.meta.env.VITE_SECRET_ID),
}

export default conf